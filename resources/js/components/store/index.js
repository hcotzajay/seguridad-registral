import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
    state: {
        //documentos: []
    },
    getters: {},
    mutations: {
        /*setDocumento: (state, payload) => (
            state.documentos.push(payload)
        ),
        spliceDocumento: (state, payload) => (
            state.documentos.splice(payload, 1)
        ),*/
    }
})
